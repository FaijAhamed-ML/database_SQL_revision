# database_SQL_revision
Create relational tables with appropriate constraints (e.g., NOT NULL, CHECK) ▪ Perform data manipulation using INSERT, UPDATE, and DELETE statements ▪ Modify table structures and enforce referential integrity using foreign key constraints ▪ Write SELECT queries to retrieve, filter, sort, from tables
